#include "MyD3D12Learning.h"


int main(HINSTANCE hInstance, HINSTANCE prevInstance, PSTR cmdLine, int showCmd)
{
	//MyDirect3DApp theApp(hInstance);
	//theApp.Initialize();
	//theApp.LogAdpt();
	//theApp.BuildAllUp();

	//theApp.Run();
	RunMyBox(hInstance, prevInstance, cmdLine, showCmd);


}